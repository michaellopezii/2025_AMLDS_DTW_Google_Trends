# Core data analysis
pandas>=1.5.0
numpy>=1.21.0

# Scientific computing and statistics
scipy>=1.9.0

# Data visualization
matplotlib>=3.5.0

# Type hints (included in Python 3.5+, but good to specify for older versions)
typing>=3.7.0

# Standard library modules (these come with Python, but listed for clarity)
# - glob (built-in)
# - os (built-in)
# - math (built-in)
# - datetime (built-in)
# - collections (built-in)

# Optional: Jupyter notebook support (if you want others to run your notebooks)
jupyter>=1.0.0
ipykernel>=6.0.0